#include <vector>

#ifndef CONSTS
#define CONSTS

const int ROWS_Y = 55;
const int COLS_X = 56;
const int CELL_SIZE = 16; 
const int DT = 200; // milliseconds

#endif

using Array2D = std::vector<std::vector<bool>>;

